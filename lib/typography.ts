/** Page title — use on `<h1>` */
export const pageTitleClassName =
  "text-3xl font-semibold tracking-tight text-gray-900";

/** Major section headings — use on `<h2>` (or `<h3>` for nested sections) */
export const sectionHeadingClassName =
  "text-lg font-medium text-gray-800";

/** In-page story labels — softer than dashboard section headers */
export const storyLabelClassName = "text-base font-medium text-gray-800";

/** Primary body copy */
export const bodyClassName = "text-[15px] leading-relaxed text-gray-700";

/** Secondary / de-emphasized copy */
export const mutedClassName = "text-gray-500";

/** Body line height + size at muted color (no gray-700 conflict) */
export const mutedBodyClassName = "text-[15px] leading-relaxed text-gray-500";

/** Quiet analytic aside — sample size, caveats */
export const confidenceNoteClassName = "text-sm italic text-gray-600";
